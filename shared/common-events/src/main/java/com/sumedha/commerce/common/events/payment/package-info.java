/**
 * Payment-domain event payloads published on {@code payment.events.v1}.
 *
 * <p>Owned by payment-service (publisher). Each record carries only the fields a downstream
 * consumer needs; it is not a copy of the {@code Payment} entity.
 */
package com.sumedha.commerce.common.events.payment;
